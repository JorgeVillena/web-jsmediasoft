import { aboutPage, contactPage, homePage, navigation } from "./simplePage";
import { project } from "./project";
import { service } from "./service";
import { siteSettings } from "./siteSettings";

export const schemaTypes = [siteSettings, homePage, service, project, aboutPage, contactPage, navigation];
